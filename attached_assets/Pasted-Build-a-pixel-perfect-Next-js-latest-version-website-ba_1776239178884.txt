Build a pixel-perfect Next.js (latest version) website based strictly on the provided Figma design. The implementation must match layout, spacing, typography, colors, and responsiveness exactly as in the Figma file.

🧱 Tech Stack Requirements
Next.js (latest App Router)
React (functional components only)
TypeScript
Tailwind CSS (strictly follow Figma styling)
NodeMailer (for email sending)
JSON-based localization system (i18n without external heavy libraries)
API routes using Next.js backend (not external backend)
Environment variables for SMTP config
🎯 Core Requirements
1. Pixel-Perfect UI
Match the Figma design 1:1 (no UI deviation allowed)
Single-page landing website
All sections included exactly as per Figma:
Hero section
Content sections
Cards / features / banners
Footer
Fully responsive (mobile, tablet, desktop exactly like Figma)
Use exact spacing, font sizes, and alignment from design
2. Form System (Important)

Create a form exactly as per Figma design with:

Input fields as per design (text, email, dropdown, etc.)
Dropdown selection support
Frontend validation (required fields + email validation)
On submit:
Show Thank You popup modal (as per Figma design)
Reset form after success
3. Email Functionality (NodeMailer)

When form is submitted:

Send email to USER
Confirmation email with submission details
Use a simple HTML template (clean default template is fine)
Send email to ADMIN
Admin email should receive full form data
Implementation:
Use /api/contact route in Next.js
Use NodeMailer
SMTP credentials should be from .env file:
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
ADMIN_EMAIL=

If SMTP is missing, fallback to mock logging (console output).

4. Localization (JSON-based i18n)

Implement localization system:

Store translations in /locales/{lang}.json
Example:
en.json
ar.json (or any second language)
All text in UI must come from JSON keys
Add language switcher (top bar or footer)
Default language: English
5. Thank You Popup Modal
Must match Figma design exactly
Trigger after successful form submission
Includes:
Success message
Close button
Auto-close optional (3–5 sec)
6. Project Structure

Keep clean separation:

/app
  /page.tsx
  /api/contact/route.ts
/components
  Hero.tsx
  Sections...
  Form.tsx
  ThankYouModal.tsx
/lib
  mailer.ts
  i18n.ts
/locales
  en.json
  xx.json
/styles
7. Code Quality Requirements
Clean, modular components
No inline hardcoded text (must use localization)
Reusable UI components
Strict TypeScript types for form data
Proper error handling in API route
Mobile-first responsive behavior
8. Bonus (If possible)
Smooth scroll between sections
Subtle animations (Framer Motion optional)
Form loading state (spinner/disabled button)
⚠️ Important
Do NOT change layout from Figma
Do NOT redesign UI
Focus on pixel-perfect accuracy
Match spacing, alignment, and typography exactly
Keep code production-ready